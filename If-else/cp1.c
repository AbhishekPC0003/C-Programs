// Write a program to accept an integer and check if it is even or odd.
#include"EvenOdd.h"
int main()
{
  int num;
  printf("Enter the number : ");
  scanf("%d",&num);
  printEvenOdd(num);
  return 0;
}